chromadb==1.5.9
streamlit>=1.30
plotly>=5.18
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
requests>=2.31
# Optional — only needed if you pick the UMAP projection in the Visualize tab.
# PCA and t-SNE work without it.
umap-learn>=0.5
# Only needed if you use the "Local model (sentence-transformers)" embedding source.
# Leave commented if you only use the in-house Jina hosted source.
# sentence-transformers>=2.5
